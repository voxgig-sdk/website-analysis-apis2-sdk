[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voxgig-sdk-website-analysis-apis2"
version = "0.0.1"
description = "Unofficial generated Python SDK for the Website Analysis APIs public API. Not affiliated with or endorsed by the upstream API provider."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
keywords = ["voxgig", "sdk", "generated-sdk", "openapi", "api-client", "website-analysis-apis2"]
dependencies = [
    "requests>=2.33",
]

[project.urls]
Homepage = "https://github.com/voxgig-sdk/website-analysis-apis2-sdk"
Repository = "https://github.com/voxgig-sdk/website-analysis-apis2-sdk"
Issues = "https://github.com/voxgig-sdk/website-analysis-apis2-sdk/issues"

# Ship the top-level single-file modules (setuptools find only discovers
# package dirs, never these) so the documented import actually installs.
# websiteanalysisapis2_types holds the generated @dataclass models.
[tool.setuptools]
py-modules = ["websiteanalysisapis2_sdk", "websiteanalysisapis2_types", "config", "features"]

# Explicit package list — setuptools auto-discovery refuses to pick when
# multiple top-level dirs (core/entity/feature/utility) are present.
[tool.setuptools.packages.find]
include = ["core*", "entity*", "feature*", "utility*"]

# Ship the PEP 561 py.typed marker(s) so the inline type hints reach consumers.
[tool.setuptools.package-data]
"*" = ["py.typed"]
